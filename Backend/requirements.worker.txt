# requirements.worker.txt

# AWS SDK
boto3

# ML and Deep Learning (CPU versions)
torch==2.1.2
torchaudio==2.1.2
torchvision==0.16.2
transformers==4.36.2
tensorboard==2.11.0

# Audio Processing
librosa==0.9.2
soundfile==0.12.1
pydub==0.25.1
# Note: 'av' is not in your original file but is often needed for audio/video with PyTorch.
# We will add it to be safe. If the Docker build fails saying it's not found, we can remove it.
# av==11.0.0 # Temporarily removed to solve build issues

# Computer Vision and Image Processing
opencv-python==4.5.5.64
opencv-contrib-python==4.6.0.66
mediapipe>=0.10.0
scikit-image>=0.21.0
pillow>=10.0.0

# Scientific Computing and Utils
numpy==1.24.3
scipy>=1.11.0
tqdm==4.64.1

# Add any other core AI dependencies from your `cascaded_backend.py` here.
psutil==5.9.8

# Web Framework (needed for health checks and APIs)
flask==3.0.0
flask-cors==4.0.0

# Environment and Utils
python-dotenv==1.0.0

# Monitoring
prometheus_client==0.19.0
matplotlib==3.7.1
noisereduce==3.0.0
werkzeug==3.0.1
yt-dlp==2023.12.30
deepface==0.0.79
speechbrain==0.5.16
scikit-learn==1.3.2
